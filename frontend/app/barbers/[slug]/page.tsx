import {BarberProfileExperience} from "@/components/site/BarberProfileExperience";export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <BarberProfileExperience slug={slug}/>}
