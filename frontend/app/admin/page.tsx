"use client"; import { useEffect,useState } from "react"; import { api } from "@/lib/api"; export default function Page(){const[d,setD]=useState<any>(null);useEffect(()=>{api<any>("/admin/dashboard").then(setD).catch(()=>{})},[]);return <div className="admin-dashboard"><header><span>LIVE OPERATIONS</span><h1>GOOD EVENING.</h1><p>Appointments, chairs, clients and content in one place.</p></header><div className="stat-grid">{[["TODAY",d?.todayAppointments],["THIS WEEK",d?.weekAppointments],["CLIENTS",d?.clients],["PENDING REVIEWS",d?.pendingReviews]].map(([a,b])=><article key={a as string}><span>{a}</span><strong>{b??"—"}</strong></article>)}</div><section className="admin-agenda"><h2>NEXT CHAIRS</h2>{d?.upcoming?.map((x:any)=><div key={x._id}><b>{new Date(x.startAt).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})}</b><span>{x.client?.name}</span><span>{x.service?.name}</span><span>{x.barber?.name}</span><em>{x.status}</em></div>)||<p>Loading agenda…</p>}</section></div>}