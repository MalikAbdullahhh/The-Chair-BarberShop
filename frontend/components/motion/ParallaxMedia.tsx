"use client";
import {EditorialImage as Image} from "@/components/media/EditorialImage";import {motion,useScroll,useTransform} from "framer-motion";import {useRef} from "react";import {useDesktopMotion} from "@/components/motion/useMotionMode";
export function ParallaxMedia({src,alt,className="",speed=14}:{src:string;alt:string;className?:string;speed?:number}){const desktop=useDesktopMotion();const ref=useRef<HTMLDivElement>(null);const{scrollYProgress}=useScroll({target:ref,offset:["start end","end start"]});const y=useTransform(scrollYProgress,[0,1],[`${-speed}%`,`${speed}%`]);const scale=useTransform(scrollYProgress,[0,.5,1],[1.08,1,1.08]);return <div ref={ref} className={`parallax-media ${className}`}><motion.div style={desktop?{y,scale}:undefined}><Image src={src} alt={alt} fill sizes="100vw"/></motion.div></div>}
