const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:5000/api";
export async function api<T>(path:string,options:RequestInit={}){const admin=typeof window!=="undefined"?localStorage.getItem("thechair_admin_token"):null;const client=typeof window!=="undefined"?localStorage.getItem("thechair_client_token"):null;const token=path.startsWith("/admin")?admin:path.startsWith("/clients/")?client:(admin||client);const r=await fetch(`${API}${path}`,{...options,headers:{"Content-Type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})}});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||"Request failed");return data as T;}
